enum CardSize { mini, small, big }

enum CardStatus { inert, unroll, roll, drop }

enum CardType { addCard, contentCreation, indexFunds, passiveIncome, privateFunds, realEstate, salaries, savingAccounts, stockAccounts }

enum MainView { carousel, primaryBigCard, secondaryBigCard }
